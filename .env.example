# Copy this file to .env, replace both values with long random secrets, and keep
# the resulting .env file outside source control.
MARIADB_ROOT_PASSWORD=replace-with-a-long-random-database-password
CRM_ADMIN_PASSWORD=replace-with-a-long-random-administrator-password
