{
 "actions": [],
 "allow_rename": 0,
 "creation": "2026-08-19 09:00:00.000000",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "action_type",
  "email_template",
  "recipient_mode",
  "recipient_address",
  "task_title",
  "task_priority",
  "task_due_offset_days",
  "notify_mode",
  "notify_user",
  "notify_role",
  "update_field",
  "update_value"
 ],
 "fields": [
  {
   "columns": 2,
   "default": "Create task",
   "description": "What this step does. Every step is executed AFTER the save commits, in the order the steps appear.",
   "fieldname": "action_type",
   "fieldtype": "Select",
   "in_list_view": 1,
   "label": "Action",
   "options": "Send email template\nCreate task\nNotify user\nUpdate field",
   "reqd": 1
  },
  {
   "depends_on": "eval:doc.action_type=='Send email template'",
   "description": "The Email Template to render. Subject and body are rendered with the record available as doc.",
   "fieldname": "email_template",
   "fieldtype": "Link",
   "in_list_view": 1,
   "label": "Email Template",
   "options": "Email Template"
  },
  {
   "default": "Record email",
   "depends_on": "eval:doc.action_type=='Send email template'",
   "description": "Record email uses the lead's or deal's own email address. Every recipient is checked against the suppression ledger before anything is queued.",
   "fieldname": "recipient_mode",
   "fieldtype": "Select",
   "label": "Send To",
   "options": "Record email\nAssigned user\nSpecific address"
  },
  {
   "depends_on": "eval:doc.action_type=='Send email template' && doc.recipient_mode=='Specific address'",
   "fieldname": "recipient_address",
   "fieldtype": "Data",
   "label": "Specific Address",
   "options": "Email"
  },
  {
   "depends_on": "eval:doc.action_type=='Create task'",
   "description": "The task title. Leave the record's own name out of it: the task links back to the record already.",
   "fieldname": "task_title",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Task Title"
  },
  {
   "default": "Medium",
   "depends_on": "eval:doc.action_type=='Create task'",
   "fieldname": "task_priority",
   "fieldtype": "Select",
   "label": "Task Priority",
   "options": "Low\nMedium\nHigh"
  },
  {
   "default": "1",
   "depends_on": "eval:doc.action_type=='Create task'",
   "description": "Days from now to the task's due date. 0 means due today.",
   "fieldname": "task_due_offset_days",
   "fieldtype": "Int",
   "label": "Due In (days)",
   "non_negative": 1
  },
  {
   "default": "Assigned user",
   "depends_on": "eval:doc.action_type=='Notify user'",
   "fieldname": "notify_mode",
   "fieldtype": "Select",
   "label": "Notify",
   "options": "Assigned user\nSpecific user\nEveryone with a role"
  },
  {
   "depends_on": "eval:doc.action_type=='Notify user' && doc.notify_mode=='Specific user'",
   "fieldname": "notify_user",
   "fieldtype": "Link",
   "label": "User",
   "options": "User"
  },
  {
   "depends_on": "eval:doc.action_type=='Notify user' && doc.notify_mode=='Everyone with a role'",
   "fieldname": "notify_role",
   "fieldtype": "Link",
   "label": "Role",
   "options": "Role"
  },
  {
   "depends_on": "eval:doc.action_type=='Update field'",
   "description": "A field of the record the rule runs on. A field this automation writes NEVER triggers another rule (depth ceiling 1).",
   "fieldname": "update_field",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Field"
  },
  {
   "depends_on": "eval:doc.action_type=='Update field'",
   "fieldname": "update_value",
   "fieldtype": "Data",
   "label": "New Value"
  }
 ],
 "index_web_pages_for_search": 1,
 "istable": 1,
 "links": [],
 "modified": "2026-08-19 09:00:00.000000",
 "modified_by": "Administrator",
 "module": "FCRM",
 "name": "CRM Workflow Action",
 "owner": "Administrator",
 "permissions": [],
 "row_format": "Dynamic",
 "sort_field": "creation",
 "sort_order": "DESC",
 "states": []
}
